# Sistema RAG Completo con Evaluación RAGAS
# Dependencias unificadas para el sistema RAG y evaluación

# Core RAG con LangChain (LCEL incluido)
langchain>=0.3.0
langchain-community>=0.3.0
langchain-chroma>=0.2.0
langchain-openai>=0.3.0
langchain-text-splitters>=0.3.0
langchain-core>=0.3.0

# Vector database (versión compatible con langchain-chroma)
chromadb>=1.0.20

# Document processing
pypdf>=4.0.0
python-dotenv>=1.0.0

# Generación (OpenAI)
openai>=1.0.0

# Embeddings locales (opcional - requiere Python <=3.12)
# NOTA: Python 3.13 no soporta PyTorch todavía
# Si usas Python 3.13, usa embedding_model="openai" en lugar de sentence-transformers
torch>=2.0.0; python_version < '3.13'
sentence-transformers>=2.0.0; python_version < '3.13'

# Evaluation RAGAS (como en el notebook clase8_RAGAS.ipynb)
ragas==0.1.9
datasets>=2.14.0

# Data processing
pandas>=1.3.0
numpy>=1.21.0,<=2.1.0

# Utilities
tqdm>=4.65.0

# Export (opcional para PDF)
reportlab>=4.0.0

# OCR para extracción de texto de imágenes en PDFs
pytesseract>=0.3.10
PyMuPDF>=1.23.0
Pillow>=9.0.0

# Hugging Face API para OCR alternativo
requests>=2.28.0
huggingface-hub>=0.16.0

# Transformers para OCR local (RECOMENDADO)
transformers>=4.30.0
torch>=2.0.0
accelerate>=0.20.0